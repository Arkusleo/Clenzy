import 'package:flutter/material.dart';

class WorkersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Workers Management - Coming Soon"));
  }
}

class BookingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Bookings Overview - Coming Soon"));
  }
}

class PanicAppsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Panic Alerts - Coming Soon"));
  }
}

class AnalyticsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Analytics Dashboard - Coming Soon"));
  }
}
